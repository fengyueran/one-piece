---
'@xinghunm/compass-ui': patch
---

移除 Storybook 相关脚本、依赖与示例文件，统一组件与开发文档；补充根入口 `ThemeProvider`、`useTheme` 导出及 `Message` 的整体清空能力，并修复 `AutoComplete`、`Form.Item`、`TreeSelect`、`Modal` 和 `Table` 文档中的若干问题。
