import styled from 'styled-components';
import { CircularSpin } from '../components';

const Container = styled.div``;

export const CircularSpinExample = () => {
  return (
    <Container>
      <CircularSpin />
    </Container>
  );
};
