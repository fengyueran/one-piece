import type { InputFieldProps } from '../input-field/types'

/**
 * Standard single-line text input props.
 *
 * `Input` is the recommended public facade for text entry in `compass-ui`.
 */
export type InputProps = InputFieldProps
