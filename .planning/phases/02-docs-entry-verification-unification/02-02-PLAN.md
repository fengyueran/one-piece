---
phase: 02-docs-entry-verification-unification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - packages/compass-ui/package.json
  - packages/compass-ui/scripts/verify-docs-public-imports.mjs
  - packages/compass-ui/docs/DEVELOPMENT.md
autonomous: true
requirements:
  - DOCS-03
  - DOCS-04
must_haves:
  truths:
    - '文档示例与自动化验证共享同一套公开导入规则'
    - '维护者可以用一个固定命令完成 docs build + 公开导入 smoke'
  artifacts:
    - 'packages/compass-ui/scripts/verify-docs-public-imports.mjs 扫描文档中的内部路径与非法 subpath'
    - '根 package.json 或 compass-ui package.json 提供稳定的 docs verify 命令'
    - 'packages/compass-ui/docs/DEVELOPMENT.md 记录文档验证流程'
  key_links:
    - 'docs smoke 必须建立在现有 dumi build 和 Phase 1 公开导入规则之上'
    - 'verify-docs-public-imports.mjs 的允许导入清单必须与 package.json.exports 一致'
---

<objective>
建立一条针对 `compass-ui` 文档与 demo 的公开导入 smoke 验证链，让示例失效可以被自动发现。

Purpose: 落地 DOCS-03 / DOCS-04，让文档、demo 与验证命令共享同一套公开导入规则。
Output: 文档公开导入扫描脚本、统一 docs verify 命令、文档验证说明。
</objective>

<execution_context>
@/Users/xinghunm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xinghunm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-docs-entry-verification-unification/02-CONTEXT.md
@.planning/phases/02-docs-entry-verification-unification/02-RESEARCH.md
@.planning/phases/02-docs-entry-verification-unification/02-VALIDATION.md
@packages/compass-ui/package.json
@packages/compass-ui/.dumirc.ts
@packages/compass-ui/docs/DEVELOPMENT.md
@packages/compass-ui/scripts/verify-public-api.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现文档公开导入 smoke 脚本</name>
  <read_first>
    packages/compass-ui/package.json
    packages/compass-ui/.dumirc.ts
    packages/compass-ui/scripts/verify-public-api.mjs
  </read_first>
  <files>
    packages/compass-ui/scripts/verify-docs-public-imports.mjs
  </files>
  <action>
    新建 `packages/compass-ui/scripts/verify-docs-public-imports.mjs`。脚本要扫描 `packages/compass-ui/docs/**/*.md` 和 `packages/compass-ui/README.md`，至少拦截三类违规：1）`src/`、`dist/` 等内部路径；2）未在 `package.json.exports` 中声明的 `@xinghunm/compass-ui/*` 子路径；3）仍依赖仓库内部 alias 才能成立的导入。允许清单以 `package.json.exports` 为准，至少包括根入口、`./theme`、`./locale`、`./icons`。脚本失败时要输出违规文件与命中的导入字符串，便于定位。
  </action>
  <verify>
    <automated>node packages/compass-ui/scripts/verify-docs-public-imports.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 脚本从 `package.json.exports` 派生允许的公开导入清单，而不是手写第二份常量
    - 运行脚本时，能够明确报告违规文件路径与具体导入字符串
    - 当前文档基线在脚本下可通过，不需要临时白名单
  </acceptance_criteria>
  <done>
    文档中的内部导入和非法 subpath 会在实现阶段被自动拦截，而不是等用户安装后才发现。
  </done>
</task>

<task type="auto">
  <name>Task 2: 接通固定 docs verify 命令并更新维护文档</name>
  <read_first>
    package.json
    packages/compass-ui/package.json
    packages/compass-ui/docs/DEVELOPMENT.md
    packages/compass-ui/scripts/verify-docs-public-imports.mjs
  </read_first>
  <files>
    package.json
    packages/compass-ui/package.json
    packages/compass-ui/docs/DEVELOPMENT.md
  </files>
  <action>
    在根 `package.json` 增加统一命令 `docs:verify:compass-ui`，把 `pnpm --filter @xinghunm/compass-ui docs:build` 和 `node packages/compass-ui/scripts/verify-docs-public-imports.mjs` 串起来。必要时在 `packages/compass-ui/package.json` 中增加包级辅助脚本，但根入口必须是唯一维护者入口。同步更新 `packages/compass-ui/docs/DEVELOPMENT.md`，新增“文档验证”章节，说明什么时候运行 `docs:verify:compass-ui`，它验证什么，以及它与 `release:verify:compass-ui` 的边界。
  </action>
  <verify>
    <automated>pnpm run docs:verify:compass-ui && rg -n 'docs:verify:compass-ui|文档验证|verify-docs-public-imports' packages/compass-ui/docs/DEVELOPMENT.md package.json</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` 中存在唯一的根级 `docs:verify:compass-ui`
    - `packages/compass-ui/docs/DEVELOPMENT.md` 说明了文档验证入口与覆盖范围
    - `pnpm run docs:verify:compass-ui` 可在本地通过并输出成功结果
  </acceptance_criteria>
  <done>
    文档 smoke 从“手动点 build”升级成仓库内固定的验证命令，维护者知道何时运行以及它验证什么。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node packages/compass-ui/scripts/verify-docs-public-imports.mjs`
- [ ] `pnpm run docs:verify:compass-ui`
- [ ] `packages/compass-ui/docs/DEVELOPMENT.md` 已记录验证流程
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- 文档与 demo 具备可重复执行的 smoke 检查入口
  </success_criteria>

<output>
After completion, create `.planning/phases/02-docs-entry-verification-unification/02-02-SUMMARY.md`
</output>
