---
phase: 01-public-api-release-baseline
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - package.json
  - .changeset/config.json
  - packages/compass-ui/package.json
  - .github/workflows/release.yml
  - packages/compass-ui/docs/DEVELOPMENT.md
autonomous: true
requirements:
  - PUBL-02
  - PUBL-05
user_setup:
  - service: npm
    why: 'trusted publishing 需要在 npm 后台把 GitHub 仓库和 release workflow 绑定为可信发布者'
    env_vars: []
    dashboard_config:
      - task: '为 @xinghunm/compass-ui 配置 Trusted Publisher'
        location: 'npm package settings -> Trusted publishers，绑定 GitHub 仓库与 .github/workflows/release.yml'
must_haves:
  truths:
    - '仓库级发布/运行时基线只由根 `package.json` 与 `.changeset/config.json` 定义；`compass-ui` 只补充自身 peer 依赖与 publishConfig'
    - '维护者可以通过固定 CI 流程发布 `compass-ui`，而不是依赖本地经验和隐性步骤'
  artifacts:
    - 'package.json 明确仓库唯一的发布前校验脚本入口与现有 packageManager / engines 基线'
    - 'packages/compass-ui/package.json 只保留 `compass-ui` 自身的 peerDependencies 与 publishConfig'
    - '.changeset/config.json 与 packages/compass-ui/package.json 的 access 配置一致；这是唯一保留的 repo 根级 access 收口点'
    - '.github/workflows/release.yml 定义 trusted publishing 发布流程'
  key_links:
    - '根 `package.json` 的发布前校验脚本必须调用 Plan 02 的 verify-public-api.mjs，而不是在包内再定义第二套入口'
    - '`.changeset/config.json` 的 access、`packages/compass-ui/package.json.publishConfig.access` 与 workflow 发布目标必须保持一致'
---

<objective>
把 `compass-ui` 的发布策略、包级运行时约束和 CI 发布入口收成一套权威配置，结束 access 与发版方式的双重真相源。

Purpose: 落地 PUBL-02 / PUBL-05，让 `compass-ui` 的发布规则、包级约束和 CI 流程都指向同一个稳定基线，同时避免把 Phase 1 扩散成整个 monorepo 的工具链升级。
Output: 统一后的 runtime/release 配置、trusted publishing workflow、中文发布说明。
</objective>

<execution_context>
@/Users/xinghunm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xinghunm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/phases/01-public-api-release-baseline/01-RESEARCH.md
@package.json
@.changeset/config.json
@packages/compass-ui/package.json
@packages/compass-ui/scripts/verify-public-api.mjs
@packages/compass-ui/docs/DEVELOPMENT.md

<interfaces>
Current release configuration:
```json
// package.json
"packageManager": "pnpm@8.6.10",
"engines": {
  "node": ">=18",
  "pnpm": ">=8"
}

// .changeset/config.json
"access": "restricted"

// packages/compass-ui/package.json
"publishConfig": { "access": "public" },
"peerDependencies": {
"react": ">=16.8.0",
"react-dom": ">=16.8.0"
}

```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 收口仓库级发布权威配置，并把 `compass-ui` 限定为包级补充配置</name>
  <read_first>
    package.json
    .changeset/config.json
    packages/compass-ui/package.json
    .planning/phases/01-public-api-release-baseline/01-RESEARCH.md
  </read_first>
  <files>
    package.json
    .changeset/config.json
    packages/compass-ui/package.json
  </files>
  <action>
    按 PUBL-02 / PUBL-05 把权威配置明确拆成两层：**仓库级唯一真相源**是根 `package.json` 与 `.changeset/config.json`，**包级补充配置**只允许留在 `packages/compass-ui/package.json` 的 `peerDependencies` 与 `publishConfig`。因此 Task 1 要在根 `package.json` 新增唯一的发布前校验入口，例如 `release:verify:compass-ui`，内容固定为 `pnpm --filter @xinghunm/compass-ui test && node packages/compass-ui/scripts/verify-public-api.mjs`，让本地与 CI 都走同一条根级入口；不要再在 `packages/compass-ui/package.json` 里新增 `release:verify`、`engines`、`packageManager` 或第二套运行时基线。

    `packages/compass-ui/package.json` 内只收口 `peerDependencies.react` 与 `react-dom` 为 `^18.2.0 || ^19.0.0`，并保留 `publishConfig.access: "public"`；`.changeset/config.json` 的 `access` 同步改为 `"public"`。这样 `access` 与发布策略依然各自只有一个仓库级读点：Changesets 读根 `.changeset/config.json`，本地/CI 校验都读根 `package.json` 脚本入口；`compass-ui` 包自身不再额外声明仓库级 `engines`、发布脚本或第二套工具链约束。不要修改根 `package.json` 现有 `packageManager` / `engines` 值，也不要把本 phase 扩展成整个 monorepo 的工具链升级。
  </action>
  <verify>
    <automated>node -e "const root=require('./package.json'); const cs=require('./.changeset/config.json'); const pkg=require('./packages/compass-ui/package.json'); if(root.packageManager!=='pnpm@8.6.10') throw new Error('root packageManager unexpectedly changed'); if(root.engines.node!=='>=18') throw new Error('root node engine unexpectedly changed'); if(root.engines.pnpm!=='>=8') throw new Error('root pnpm engine unexpectedly changed'); if(!root.scripts['release:verify:compass-ui']) throw new Error('missing root release verify'); if(cs.access!=='public') throw new Error('changeset access'); if(pkg.publishConfig.access!=='public') throw new Error('publishConfig access'); if(pkg.peerDependencies.react!=='^18.2.0 || ^19.0.0') throw new Error('react peer'); if(pkg.peerDependencies['react-dom']!=='^18.2.0 || ^19.0.0') throw new Error('react-dom peer'); if(pkg.scripts && pkg.scripts['release:verify']) throw new Error('package-level release verify should not exist'); if(pkg.engines) throw new Error('package-level engines should not exist');"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n '"access": "public"' .changeset/config.json packages/compass-ui/package.json` 至少命中 2 条
    - `rg -n '"react": "\\^18.2.0 \\|\\| \\^19.0.0"|"react-dom": "\\^18.2.0 \\|\\| \\^19.0.0"' packages/compass-ui/package.json` 至少命中 2 条
    - `rg -n '"release:verify:compass-ui"' package.json` 命中 1 条，说明发布前校验入口收敛在根 `package.json`
    - `! rg -n '"release:verify"' packages/compass-ui/package.json` 成功，说明 `compass-ui` 包内没有第二套发布前校验入口
    - `! rg -n '"engines"' packages/compass-ui/package.json` 成功，说明包级运行时基线没有与根级配置重复
    - `node -e "const root=require('./package.json'); if(root.packageManager!=='pnpm@8.6.10'||root.engines.node!=='>=18'||root.engines.pnpm!=='>=8') throw new Error('root package unexpectedly changed')"` 成功，说明 Task 1 没把范围扩散到根工具链升级
  </acceptance_criteria>
  <done>
    仓库级发布真相源被收敛到根 `package.json` 与 `.changeset/config.json`，`compass-ui` 只保留包自身需要的 peer/access 配置。
  </done>
</task>

<task type="auto">
  <name>Task 2: 建立 trusted publishing release workflow 并写清中文发布流程</name>
  <read_first>
    .changeset/config.json
    packages/compass-ui/package.json
    packages/compass-ui/scripts/verify-public-api.mjs
    packages/compass-ui/docs/DEVELOPMENT.md
  </read_first>
  <files>
    .github/workflows/release.yml
    packages/compass-ui/docs/DEVELOPMENT.md
  </files>
  <action>
    新建 `.github/workflows/release.yml`，工作流名固定为 `release`，触发条件使用 `workflow_dispatch` 和 `push` 到 `master`；权限至少包含 `contents: read` 与 `id-token: write`。步骤使用 `actions/checkout@v4`、`pnpm/action-setup@v4`（版本固定为根 `package.json.packageManager` 对应的 `8.6.10`，不另起第二套 pnpm 规则）、`actions/setup-node@v4`（`node-version: 22.14.0`、`registry-url: https://registry.npmjs.org`、`cache: pnpm`）。这里的 `22.14.0` 只表示 trusted publishing 所需的 release job runtime，不是新的消费者兼容性基线；仓库的兼容性真相源仍是根 `package.json.engines`。安装依赖后执行 `pnpm install --frozen-lockfile`、`pnpm run release:verify:compass-ui`，最后通过 `changesets/action@v1` 调用 `changeset publish` 发布。不要写入 npm token；发布凭证只依赖 trusted publishing。

    同步更新 `packages/compass-ui/docs/DEVELOPMENT.md`，新增中文章节“公开发布流程”，明确列出：1）本地预检命令 `pnpm run release:verify:compass-ui`，并说明它内部会调用 `packages/compass-ui/scripts/verify-public-api.mjs`；2）如何提交 changeset；3）CI workflow 会自动执行的步骤；4）npm 后台需要配置 Trusted Publisher 的页面路径与工作流文件名 `.github/workflows/release.yml`。
  </action>
  <verify>
    <automated>test -f .github/workflows/release.yml && rg -n 'id-token: write|changesets/action@v1|release:verify:compass-ui|workflow_dispatch|push:|22.14.0|8.6.10' .github/workflows/release.yml && rg -n '公开发布流程|Trusted Publisher|verify-public-api.mjs|release:verify:compass-ui|changeset' packages/compass-ui/docs/DEVELOPMENT.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'id-token: write' .github/workflows/release.yml` 命中 1 条
    - `rg -n 'changesets/action@v1' .github/workflows/release.yml` 命中 1 条
    - `rg -n 'release:verify:compass-ui' .github/workflows/release.yml package.json` 至少命中 2 条
    - `rg -n '22.14.0|8.6.10' .github/workflows/release.yml` 至少命中 2 条，说明 workflow 的 Node/pnpm 选择是显式的、可解释的
    - `rg -n 'verify-public-api.mjs' packages/compass-ui/docs/DEVELOPMENT.md` 命中 1 条
    - `rg -n '公开发布流程|Trusted Publisher|release:verify:compass-ui' packages/compass-ui/docs/DEVELOPMENT.md` 至少命中 3 条
  </acceptance_criteria>
  <done>
    仓库拥有可重复执行的 CI 发布流和中文维护手册，发布步骤不再依赖口头经验。
  </done>
</task>

</tasks>

<verification>
先运行 Task 1 的根级发布入口 / access / peer 配置断言，再执行 `test -f .github/workflows/release.yml` 与 `rg` 校验 workflow/文档关键字段。若本地环境允许，也可额外运行 `pnpm run release:verify:compass-ui` 作为 workflow 前置验收。
</verification>

<success_criteria>
PUBL-02 的权威配置冲突被消除，且修复范围没有扩散成整个 monorepo 升级；PUBL-05 的根级 release 入口和 CI workflow 形成固定路径；维护者只需按中文说明配置 Trusted Publisher 并提交 changeset 即可发布。
</success_criteria>

<output>
After completion, create `.planning/phases/01-public-api-release-baseline/01-public-api-release-baseline-03-SUMMARY.md`
</output>
```
