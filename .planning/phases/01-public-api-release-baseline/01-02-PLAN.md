---
phase: 01-public-api-release-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - packages/compass-ui/scripts/verify-public-api.mjs
  - packages/compass-ui/test-consumer/package.json
  - packages/compass-ui/test-consumer/tsconfig.json
  - packages/compass-ui/test-consumer/src/index.tsx
autonomous: true
requirements:
  - PUBL-03
  - PUBL-04
must_haves:
  truths:
    - '发布前可以自动验证 `npm pack` 产物、类型分发和公开 exports 是否真实可用'
    - '最小消费者夹具可以只通过公开包名完成安装与类型检查'
  artifacts:
    - 'packages/compass-ui/scripts/verify-public-api.mjs 串联 publint、npm pack 与消费者 smoke test'
    - 'packages/compass-ui/test-consumer/* 形成独立最小消费者夹具'
  key_links:
    - 'verify-public-api.mjs 必须把打包出来的 tgz 安装到 test-consumer，而不是引用源码目录'
    - 'test-consumer/src/index.tsx 必须只导入公开包名、./theme、./locale、./icons'
---

<objective>
把 Phase 1 的发布验证从“人工判断”升级成可重复执行的自动 smoke test，直接验证打包产物与外部消费者路径。

Purpose: 落地 PUBL-03 / PUBL-04，让维护者在真正发布前就能证明 `compass-ui` 的 tarball、类型和公开导入可用。
Output: 自动验证脚本与最小消费者夹具。
</objective>

<execution_context>
@/Users/xinghunm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xinghunm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-public-api-release-baseline/01-RESEARCH.md
@.planning/phases/01-public-api-release-baseline/01-VALIDATION.md
@packages/compass-ui/package.json
@packages/compass-ui/tsconfig.json
@packages/compass-ui/jest.config.js
@packages/compass-ui/src/index.ts
@packages/compass-ui/src/theme/index.ts
@packages/compass-ui/src/locale/index.ts

<interfaces>
Public imports expected after Plan 01:
```ts
import { Button, ThemeProvider } from '@xinghunm/compass-ui'
import { defaultTheme } from '@xinghunm/compass-ui/theme'
import { zhCN } from '@xinghunm/compass-ui/locale'
import { SearchIcon } from '@xinghunm/compass-ui/icons'
```

Current package scripts:

```json
"build": "tsup src/index.ts --format cjs,esm --dts --external react,react-dom --outDir dist --clean --minify",
"test": "jest"
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现打包产物校验脚本</name>
  <read_first>
    packages/compass-ui/package.json
    packages/compass-ui/tsconfig.json
    .planning/phases/01-public-api-release-baseline/01-VALIDATION.md
  </read_first>
  <files>
    packages/compass-ui/scripts/verify-public-api.mjs
  </files>
  <action>
    新建 `packages/compass-ui/scripts/verify-public-api.mjs`，并把它写成**与调用 cwd 无关**的 Node 脚本：脚本入口必须先通过 `import.meta.url`、`fileURLToPath` 和 `path.resolve` 计算出 `script_dir`、`package_dir`、`repo_root`、`consumer_dir`，后续所有命令都基于这些绝对路径执行，而不是假设当前工作目录在仓库根。这样无论它被 `node packages/compass-ui/scripts/verify-public-api.mjs` 直接调用，还是被根脚本 / CI workflow 调用，都能落在同一套路径契约上。

    脚本内部串联四个明确步骤：1）在 `repo_root` 执行 `pnpm --filter @xinghunm/compass-ui build`；2）执行 `pnpm dlx publint <package_dir>`；3）显式绑定 `packages/compass-ui` 作为打包目标，例如 `pnpm --dir <package_dir> pack --pack-destination <temp-dir>`，获取最新 tgz 路径；4）把该 tgz 安装到 `<consumer_dir>` 后执行 `pnpm --dir <consumer_dir> exec tsc --noEmit`。不要使用依赖当前 cwd 的 `pnpm pack`，也不要让脚本在仓库根打出其他 workspace 包。脚本需要在每步失败时抛出带步骤名的错误，并在结束时清理临时目录。此任务只要求把脚本逻辑静态落地，不在这里执行依赖 `test-consumer` 夹具的 smoke test；真实运行留到 Task 2。

  </action>
  <verify>
    <automated>bash -lc 'rg -n "import.meta.url|fileURLToPath" packages/compass-ui/scripts/verify-public-api.mjs && rg -n "pnpm dlx publint" packages/compass-ui/scripts/verify-public-api.mjs && rg -n "pnpm --dir .*pack --pack-destination" packages/compass-ui/scripts/verify-public-api.mjs && rg -n "test-consumer.*tsc --noEmit|--dir .*test-consumer.*tsc --noEmit" packages/compass-ui/scripts/verify-public-api.mjs && rg -n "throw new Error\\(" packages/compass-ui/scripts/verify-public-api.mjs'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "import.meta.url|fileURLToPath" packages/compass-ui/scripts/verify-public-api.mjs` 至少命中 1 条，说明脚本不依赖调用 cwd
    - `rg -n "pnpm dlx publint" packages/compass-ui/scripts/verify-public-api.mjs` 命中 1 条
    - `rg -n "pnpm --dir .*pack --pack-destination" packages/compass-ui/scripts/verify-public-api.mjs` 命中 1 条，说明打包目标被显式绑定到 `packages/compass-ui`
    - `rg -n "test-consumer.*tsc --noEmit|--dir .*test-consumer.*tsc --noEmit" packages/compass-ui/scripts/verify-public-api.mjs` 命中 1 条
    - `rg -n "throw new Error\\(" packages/compass-ui/scripts/verify-public-api.mjs` 至少命中 1 条，确保失败会带诊断信息
  </acceptance_criteria>
  <done>
    一个命令即可在固定路径契约下完成 publint、打包产物检查和消费者类型 smoke test。
  </done>
</task>

<task type="auto">
  <name>Task 2: 建立最小消费者夹具并只使用公开导入路径</name>
  <read_first>
    packages/compass-ui/src/index.ts
    packages/compass-ui/src/theme/index.ts
    packages/compass-ui/src/locale/index.ts
    packages/compass-ui/src/icons/index.ts
    packages/compass-ui/scripts/verify-public-api.mjs
  </read_first>
  <files>
    packages/compass-ui/test-consumer/package.json
    packages/compass-ui/test-consumer/tsconfig.json
    packages/compass-ui/test-consumer/src/index.tsx
  </files>
  <action>
    新建不纳入 workspace 的最小消费者夹具 `packages/compass-ui/test-consumer`。`package.json` 使用 `private: true`、`type: "module"`，固定依赖 `react@18.2.0`、`react-dom@18.2.0`，并把 `typescript@5.2.2`、`@types/react@18.2.22`、`@types/react-dom@18.2.7` 放到 `devDependencies`。`tsconfig.json` 使用 `jsx: "react-jsx"`、`module: "NodeNext"`、`moduleResolution: "NodeNext"`、`strict: true`、`noEmit: true`。`src/index.tsx` 必须同时验证四条公开导入：从 `@xinghunm/compass-ui` 导入 `Button` 与 `ConfigProvider`，从 `@xinghunm/compass-ui/theme` 导入 `defaultTheme`，从 `@xinghunm/compass-ui/locale` 导入 `zhCN`，从 `@xinghunm/compass-ui/icons` 导入至少一个真实图标导出（例如 `SearchIcon` 或 `CloseIcon`）。示例代码里要把该图标实际参与 JSX 或类型推导，避免“只写 import 但未被编译器验证”。不要引用任何源码相对路径，也不要导入未在 Plan 01 公开的组件级 subpath。
  </action>
  <verify>
    <automated>node packages/compass-ui/scripts/verify-public-api.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\"type\": \"module\"" packages/compass-ui/test-consumer/package.json` 命中 1 条
    - `rg -n "\"module\": \"NodeNext\"|\"moduleResolution\": \"NodeNext\"" packages/compass-ui/test-consumer/tsconfig.json` 至少命中 2 条
    - `rg -n "@xinghunm/compass-ui/theme|@xinghunm/compass-ui/locale|@xinghunm/compass-ui/icons|@xinghunm/compass-ui'" packages/compass-ui/test-consumer/src/index.tsx` 至少命中 4 条
    - `rg -n "@xinghunm/compass-ui/icons" packages/compass-ui/test-consumer/src/index.tsx` 命中 1 条，确保 `./icons` subpath 被真实覆盖
    - `rg -n "(src/|dist/)" packages/compass-ui/test-consumer/src/index.tsx` 无匹配
  </acceptance_criteria>
  <done>
    仓库内存在一个只走公开入口的消费者夹具，根入口、`./theme`、`./locale`、`./icons` 都会通过打包 tgz 安装后的类型检查。
  </done>
</task>

</tasks>

<verification>
运行 `node packages/compass-ui/scripts/verify-public-api.mjs`。成功标准是：`publint` 通过、打包 tgz 生成成功、消费者夹具安装成功、`tsc --noEmit` 通过。
</verification>

<success_criteria>
发布验证链从 `build/test` 扩展为 `publint + npm pack + consumer smoke test`；PUBL-03 明确落地，PUBL-04 由真实 tgz 安装再次验证。
</success_criteria>

<output>
After completion, create `.planning/phases/01-public-api-release-baseline/01-public-api-release-baseline-02-SUMMARY.md`
</output>
