---
phase: 04-complex-interaction-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/compass-ui/src/select/select.tsx
  - packages/compass-ui/src/select/select.test.tsx
  - packages/compass-ui/src/select/types.ts
  - packages/compass-ui/docs/components/select.md
  - packages/compass-ui/docs/API.md
autonomous: true
requirements:
  - INTR-01
must_haves:
  truths:
    - '用户可以仅用键盘完整打开、导航、选择并关闭 Select'
    - 'Select 的 combobox/listbox 语义和 aria 暴露对外稳定可测'
  artifacts:
    - 'select.tsx 中稳定的键盘状态与 aria 语义'
    - 'select.test.tsx 中键盘导航和关闭回归'
    - 'select.md 中只记录已验证的键盘行为'
  key_links:
    - 'Select 的键盘模型会成为后续复杂选择器的参考'
    - '文档承诺不能超出自动化覆盖'
---

<objective>
补齐 `Select` 的完整键盘导航、激活项管理和基础 `aria` 语义，让它从“能点开”升级为“键盘可完整操作”的选择器。

Purpose: 落地 `INTR-01`，先把复杂交互里最核心也最脆弱的 `Select` 稳定下来。
Output: 完整键盘模型、更新后的测试和文档说明。
</objective>

<execution_context>
@/Users/xinghunm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/xinghunm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-complex-interaction-stabilization/04-CONTEXT.md
@.planning/phases/04-complex-interaction-stabilization/04-RESEARCH.md
@.planning/phases/04-complex-interaction-stabilization/04-VALIDATION.md
@packages/compass-ui/src/select/select.tsx
@packages/compass-ui/src/select/select.test.tsx
@packages/compass-ui/docs/components/select.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为 Select 建立完整键盘状态机和 combobox/listbox 语义</name>
  <read_first>
    packages/compass-ui/src/select/select.tsx
    packages/compass-ui/src/select/select.test.tsx
    .planning/codebase/CONCERNS.md
  </read_first>
  <files>
    packages/compass-ui/src/select/select.tsx
    packages/compass-ui/src/select/select.test.tsx
    packages/compass-ui/src/select/types.ts
  </files>
  <action>
    让 `Select` 支持键盘打开、`ArrowUp/ArrowDown` 导航、`Enter` 或 `Space` 选择、`Escape` 关闭，以及必要的 `aria-expanded`、`aria-controls`、`aria-activedescendant` 等暴露。要覆盖普通模式、搜索模式和禁用态，但不要在这个 plan 中同时去重构 `TreeSelect` 或其他浮层组件。
  </action>
  <verify>
    <automated>pnpm --filter @xinghunm/compass-ui test -- --runInBand src/select/select.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `Select` 能通过键盘完成完整选择流程
    - 新增测试验证真实用户键盘行为，而不是实现细节
    - 文档里当前已有的键盘承诺与实现一致
  </acceptance_criteria>
  <done>
    `Select` 不再只适合鼠标用户，而成为一个真正可键盘操作的基础选择器。
  </done>
</task>

<task type="auto">
  <name>Task 2: 把已验证的 Select 键盘行为写回文档和 API 说明</name>
  <read_first>
    packages/compass-ui/docs/components/select.md
    packages/compass-ui/docs/API.md
    packages/compass-ui/src/select/select.test.tsx
  </read_first>
  <files>
    packages/compass-ui/docs/components/select.md
    packages/compass-ui/docs/API.md
  </files>
  <action>
    在 `select.md` 中新增或更新“键盘与可访问性”说明，只记录已经被测试覆盖的行为。同步补充 `API.md` 中与 `Select` 交互有关的对外契约说明，例如搜索输入、展开状态和选择确认方式。
  </action>
  <verify>
    <automated>pnpm run docs:verify:compass-ui && rg -n '键盘|Arrow|Enter|Escape|combobox' packages/compass-ui/docs/components/select.md</automated>
  </verify>
  <acceptance_criteria>
    - 文档说明与自动化测试覆盖一致
    - API 页没有承诺未实现的高级键盘行为
  </acceptance_criteria>
  <done>
    `Select` 的键盘模型对外可理解、可复制、可验证。
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @xinghunm/compass-ui test -- --runInBand src/select/select.test.tsx`
- [ ] `pnpm run docs:verify:compass-ui`
- [ ] `select.md` 只承诺已经被验证覆盖的键盘行为
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `INTR-01` 达成
  </success_criteria>

<output>
After completion, create `.planning/phases/04-complex-interaction-stabilization/04-01-SUMMARY.md`
</output>
